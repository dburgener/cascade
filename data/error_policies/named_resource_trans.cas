resource foo {
    resource_transition(this, domain, bar, [file dir], "something/test.txt");

    // Policies must include at least one av rule
    allow(domain, foo, file, [read]);
}

resource bar {}